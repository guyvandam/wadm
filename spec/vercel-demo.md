# vercel demo
- each WADM view includes an import & export button for the data in json.
- data is stored in localStorage and is deleted upon refreshing the page.
    - shows a prompt to user to validate refresh (and thus deletion) intent.
    - returns to home screen.